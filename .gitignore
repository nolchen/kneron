# Dependencies
backend/.venv/
frontend/node_modules/
frontend/.next/

# Env (contains API keys)
backend/.env

# Hermes Agent — large cloned repo, reference only
hermes-agent/

# macOS
.DS_Store
*\ 2.*
*\ 2/

# Python cache
__pycache__/
*.pyc

# Local databases (per-deployment data, not source)
backend/notes_db/
backend/pm_data.db

# Obsidian settings
PM-Vault/.obsidian/

# Private/secret notes + tooling — kept fully local, never committed
.notes.md
.notes.md.enc
.secret*
.private/
secret.sh

# Generated demo status reports (runtime artifacts, not source)
PM-Vault/PM-Agent/Team Status Report *
