# Dependencies
backend/.venv/
frontend/node_modules/
frontend/.next/

# Env (contains API keys)
backend/.env

# Hermes Agent — large cloned repo, reference only
hermes-agent/

# macOS
.DS_Store
*\ 2.*
*\ 2/

# Python cache
__pycache__/
*.pyc

# IDE
.idea/
.vscode/

# Local databases (per-deployment data, not source)
backend/notes_db/
backend/pm_data.db

# Obsidian settings
PM-Vault/.obsidian/

# Private notes: the PLAINTEXT and the passphrase never get committed.
# The encrypted blob (.notes.md.enc) and the secret.sh tool ARE committed on purpose
# (encrypted-at-rest secret + the easter egg). Key lives in macOS Keychain, not the repo.
.notes.md
.secret*
.private/

# Generated demo status reports (runtime artifacts, not source)
PM-Vault/PM-Agent/Team Status Report *
PM-Vault/PM-Agent/Status report *
